on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  build:
    name: Build
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@master
    - name: Build the docker-compose stack
      run: sudo docker-compose --verbose -f docker-compose.yml up --force-recreate -d
    - name: Check running containers
      run: sudo docker ps -a
    - name: Check logs
      run: sudo docker-compose logs